{% set version = "1.1.0" %}
{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-rsconnect
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/rsconnect_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/rsconnect/rsconnect_{{ version }}.tar.gz
  sha256: ec9e660b563160786a0dc8df9589ceaef1c25e8a5bb49ecc654362273a4bffca

build:
  merge_build_host: true  # [win]
  number: 0
  noarch: generic
  rpaths:
    - lib/R/lib/
    - lib/

requirements:
  build:
    - {{ posix }}zip               # [win]
  host:
    - r-base
    - r-cli
    - r-curl
    - r-digest
    - r-jsonlite
    - r-lifecycle
    - r-openssl >=2.0.0
    - r-packrat >=0.6
    - r-renv >=1.0.0
    - r-rlang >=1.0.0
    - r-rstudioapi >=0.5
    - r-yaml >=2.1.5
  run:
    - r-base
    - r-cli
    - r-curl
    - r-digest
    - r-jsonlite
    - r-lifecycle
    - r-openssl >=2.0.0
    - r-packrat >=0.6
    - r-renv >=1.0.0
    - r-rlang >=1.0.0
    - r-rstudioapi >=0.5
    - r-yaml >=2.1.5

test:
  commands:
    - $R -e "library('rsconnect')"           # [not win]
    - "\"%R%\" -e \"library('rsconnect')\""  # [win]

about:
  home: https://github.com/rstudio/rsconnect
  license: GPL-2.0-only
  summary: Programmatic deployment interface for 'RPubs', 'shinyapps.io', and 'Posit Connect'. Supported content types include R Markdown documents, Shiny applications, Plumber APIs, plots, and static web content.
  license_family: GPL2
  license_file:
    - {{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-2

extra:
  recipe-maintainers:
    - conda-forge/r
